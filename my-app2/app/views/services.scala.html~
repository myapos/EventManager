@(locations: List[Location], username: String, role:String)
@main(title = "Services Page" , null) {

<h1>Services page</h1>
<p>Hello from Services Page</p>
@for(location <- locations){
    @location.id <br>
    @location.name <br>
    @location.address <br>
    @location.latitude <br>
    @location.longitude <br>
    
}
}